{
  "name": "auto-merge-action",
  "version": "1.0.0",
  "description": "A GitHub Action to automatically create and merge pull requests.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write ."
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/owen-6936/auto-merge-action.git"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
